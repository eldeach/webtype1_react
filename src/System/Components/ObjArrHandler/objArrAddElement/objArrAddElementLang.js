const langConfig = {
    alertMsg : {
        pkDuplicated : {
            kor : "이 값은 이미 추가되어 있습니다. ( ",
            eng : "This value has already been added. ( "
        }
    },
}

export default langConfig;